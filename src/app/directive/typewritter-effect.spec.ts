import { TypewritterEffect } from './typewritter-effect';

describe('TypewritterEffect', () => {
  it('should create an instance', () => {
    const directive = new TypewritterEffect();
    expect(directive).toBeTruthy();
  });
});
